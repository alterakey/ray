<?xml version="1.0"?>
<project name="runner">
  <loadproperties srcFile="secure.properties" />

  <!-- quick check on runner.dir -->
  <fail
          message="runner.dir is missing. Make sure to inject it through an env var"
          unless="runner.dir"
  />

  <import file="${runner.dir}/build.xml" />
</project>
